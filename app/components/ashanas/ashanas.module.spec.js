'use strict';

/**
 * Karma unit tests.
 */
describe('ashanas module', function () {

  beforeEach(module('yogaSequence.ashanas'));

   var $httpBackend,
       ashanassState;

   beforeEach(function () {
     inject(function($injector) {
      // injects
     });
   });

  it('should be tested', function() {
    // test me
  });

});
