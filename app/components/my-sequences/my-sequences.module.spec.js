'use strict';

/**
 * Karma unit tests.
 */
describe('mySequences module', function () {

  beforeEach(module('yogaSequence.mySequences'));

   var $httpBackend,
       mySequencessState;

   beforeEach(function () {
     inject(function($injector) {
      // injects
     });
   });

  it('should be tested', function() {
    // test me
  });

});
