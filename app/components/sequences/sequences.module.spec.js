'use strict';

/**
 * Karma unit tests.
 */
describe('sequences module', function () {

  beforeEach(module('yogaSequence.sequences'));

   var $httpBackend,
       sequencessState;

   beforeEach(function () {
     inject(function($injector) {
      // injects
     });
   });

  it('should be tested', function() {
    // test me
  });

});
