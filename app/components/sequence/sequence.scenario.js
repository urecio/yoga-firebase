'use strict';

describe('sequence e2e tests', function () {
  describe('basic info', function () {
    it('should have title', function () {

    });
    it('should have description', function () {

    });
    it('should have categories', function () {

    });
    it('should have the list of ashans', function () {

    });
    it('should have the duration', function () {
      // the duration could be calculated automatically by the number of breathings
    });
  });

  describe('ashanas', function () {
    it('should have a list of ashanas', function () {

    });
    it('should have the type of breathing on each ashana', function () {
      // THIS could be indicated by a big arroW up/down
    });
    it('should have the number of breathings', function () {
      // indicated with a number close to the arrow
    });
    it('could have a breathing speed (showed with a signal strength icon)', function () {
      // just with fast, medium and slow should be enough
    });
    it('could have notes on each ashana', function () {

    });
  });

  describe('interaction', function () {
    it('should be able to save to favs', function () {

    });
    it('should be shareable on social media', function () {

    });
    it('should have private/public mode', function () {
      // obviously if it's private, it's not reachable by other users...
    });
    describe('fork', function () {
      it('should be able to fork it', function () {

      });
      it('should change the sequence url by forking it', function () {

      });
      it('should become editable', function () {

      });
    });
  });

});
