'use strict';

/**
 * Karma unit tests.
 */
describe('baseLayout module', function () {

  beforeEach(module('yogaSequence.baseLayout'));

   var $httpBackend,
       baseLayoutsState;

   beforeEach(function () {
     inject(function($injector) {
      // injects
     });
   });

  it('should be tested', function() {
    // test me
  });

});
